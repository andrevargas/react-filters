import React from 'react';

const FilterActions = props => (
    <div className="FilterActions">
        <button className="FilterActions__search">Search</button>
    </div>
);

export default FilterActions;
